const login_form = document.querySelector("div.login-wrapper.wrapper");
const login_username = document.querySelector(`#login-username-input`);
const login_password = document.querySelector(`#login-password-input`);

const register_form = document.querySelector(`div.register-wrapper.wrapper`);
const register_username = document.querySelector(`#register-username-input`);
const register_firstname = document.querySelector(`#register-first-name-input`);
const register_password = document.querySelector(`#register-password-input`);

const finish_buttons = document.querySelectorAll(`button.finish`);

const switch_form_buttons = document.querySelectorAll("span.switch-form");